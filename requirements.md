# Tenjin AI Requirements

To run Tenjin AI locally, you need the following dependencies:

### 1. Prerequisites
* **Python 3.x** (Ensure it is added to your system PATH)
* **pip** (Python package manager)

### 2. Required Libraries
Install the necessary backend dependencies by running:
```bash
pip install flask flask-cors requests